# MigraTech Presentations

This is a monorepo that contains presentations.

To install dependencies, run:

```shell
npm install
```

To start the development server, go to the presentation folder and run:

```shell
npm start
```